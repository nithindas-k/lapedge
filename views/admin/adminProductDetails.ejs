<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Details</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/admin.css">
    <link rel="stylesheet" href="/css/productDetails.css">
</head>

<body>
    <div class="main-div">
        <%- include("../partials/adminNavbar") %>
            <div class="hello">
                <%- include('../partials/adminSidebar') %>
            </div>
            <div class="containerProductDetails">
                <h1>Product Information</h1>

                <div class="product-details">
                    <p><strong>Product Name:</strong> 
                        <%= product.name %>
                    </p>
                    <p><strong>Description:</strong> 
                        <%= product.description %>
                    </p>
                    <p><strong>Brand:</strong> 
                        <%= product.brand %>
                    </p>
                    <p><strong>Category:</strong> 
                        <%= product.category.name %> 
                    </p>
                    <p><strong>Regular Price:</strong> 
                        ₹<%= product.regularPrice %>
                    </p>
                    <p><strong>Sale Price:</strong> 
                        ₹<%= product.salePrice %>
                    </p>
                    <p><strong>Quantity:</strong> 
                        <%= product.quantity %>
                    </p>
                    <p><strong>Status:</strong> 
                        <%= product.status %>
                    </p>
                    <h3>Specifications:</h3>
                    <ul>
                        <li><strong>RAM:</strong> <%= product.specifications.value%></li>
                        <li><strong>Processor:</strong> <%= product.specifications.processor.value%></li>
                        <li><strong>Display Size:</strong> <%= product.specifications.displaySize.value%></li>
                        <li><strong>Storage:</strong> <%= product.specifications.storage.value %></li>
                    </ul>
                    <h3>Images:</h3>
                    <div class="product-images">
                        <% product.productImage.forEach((image, index) => { %>
                            <img src="<%= image %>" alt="Product Image <%= index + 1 %>" class="product-image" width="150">
                        <% }) %>
                    </div>
                </div>

                <a href="/admin/products" class="back-button" style="color: #000000; text-decoration: none;">Back to Products</a>
            </div>
    </div>
    <script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>
</body>

</html>
