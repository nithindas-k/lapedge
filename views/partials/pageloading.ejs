<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Loading Example</title>
    <style>
        :root {
            --bg: hsl(223, 10%, 90%);
            --fg: hsl(223, 10%, 10%);
            --c1: hsl(33, 100%, 50%);
            --c2: hsl(0, 0%, 100%);
            --c3: hsl(34, 100%, 50%);
            
            /* Global timing variables */
            --bounce-duration: 1s;
            --bounce-delay-1: 0s;
            --bounce-delay-2: 0.1s;
            --bounce-delay-3: 0.2s;
        }

       
        .loading-container {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 1000;
            display: flex;
            justify-content: center;
            align-items: center;
            backdrop-filter: blur(10px); 
            -webkit-backdrop-filter: blur(10px);
        }

        .pl1 {
            display: flex;
            justify-content: space-around;
            width: 6em;
            height: 6em;
            margin: 1.5em;
        }

        .pl1__a, .pl1__b, .pl1__c {
            border-radius: 50%;
            width: 1em;
            height: 1em;
            transform-origin: 50% 100%;
        }

        .pl1__a {
            background: var(--c1);
            animation: bounce1 var(--bounce-duration) linear infinite;
            animation-delay: var(--bounce-delay-1);
        }

        .pl1__b {
            background: var(--c2);
            animation: bounce1 var(--bounce-duration) linear infinite;
            animation-delay: var(--bounce-delay-2);
        }

        .pl1__c {
            background: var(--c3);
            animation: bounce1 var(--bounce-duration) linear infinite;
            animation-delay: var(--bounce-delay-3);
        }

        /* Animations */
        @keyframes bounce1 {
            from, to {
                transform: translateY(0) scale(1, 1);
                animation-timing-function: ease-in;
            }
            45% {
                transform: translateY(5em) scale(1, 1);
                animation-timing-function: linear;
            }
            50% {
                transform: translateY(5em) scale(1.5, 0.5);
                animation-timing-function: linear;
            }
            55% {
                transform: translateY(5em) scale(1, 1);
                animation-timing-function: ease-out;
            }
        }

        /* Dark theme */
        @media (prefers-color-scheme: dark) {
            :root {
                --bg: hsl(223, 10%, 10%);
                --fg: hsl(223, 10%, 90%);
            }
        }
    </style>
</head>
<body>

    <!-- Loading animation -->
    <div class="loading-container" id="loading-container">
        <div class="pl1">
            <div class="pl1__a"></div>
            <div class="pl1__b"></div>
            <div class="pl1__c"></div>
            
        </div>


        
       
    </div>

    <script>
        // Set a timeout of 3 seconds to hide the loading container
        setTimeout(() => {
            document.getElementById('loading-container').style.display = 'none';
        }, 850); // After 3000ms (3 seconds), hide the loading container
    </script>
</body>
</html>
