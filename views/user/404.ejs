<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Page Not Found | LapEdge</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #000;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
        }

        .container {
            text-align: center;
            padding: 2rem;
            max-width: 800px;
        }

        .error-code {
            font-size: 120px;
            font-weight: bold;
            color: #FFA000;
            margin-bottom: 1rem;
            text-shadow: 2px 2px 10px rgba(255, 160, 0, 0.3);
            animation: glitch 3s infinite;
        }

        @keyframes glitch {
            0%, 100% { transform: translate(0); }
            20% { transform: translate(-2px, 2px); }
            40% { transform: translate(-2px, -2px); }
            60% { transform: translate(2px, 2px); }
            80% { transform: translate(2px, -2px); }
        }

        .logo-container {
            width: 200px;
            height: 200px;
            margin: 2rem auto;
            animation: float 3s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        .logo-container img {
            width: 100%;
            height: 100%;
            object-fit: contain;
        }

        .message {
            color: #fff;
            font-size: 24px;
            margin: 2rem 0;
        }

        .sub-message {
            color: #999;
            font-size: 16px;
            margin-bottom: 2rem;
        }

        .btn {
            display: inline-block;
            padding: 15px 30px;
            background-color: #FFA000;
            color: #000;
            text-decoration: none;
            border-radius: 50px;
            font-weight: bold;
            transition: all 0.3s ease;
        }

        .btn:hover {
            background-color: #FFB300;
            transform: scale(1.05);
            box-shadow: 0 0 20px rgba(255, 160, 0, 0.3);
        }

        @media (max-width: 768px) {
            .container {
                padding: 1rem;
            }
            .error-code {
                font-size: 80px;
            }
            .message {
                font-size: 20px;
            }
            .logo-container {
                width: 150px;
                height: 150px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="error-code">404</div>
        <div class="logo-container">
            <img src="/images/mainlogo.png" alt="LapEdge Logo">
        </div>
        <div class="message">Oops! Page Not Found</div>
        <div class="sub-message">The page you're looking for seems to have vanished into cyberspace.</div>
        
    </div>
</body>
</html>